# Development notes

`PATCH_NOTES_*.txt` are the original Korean release notes kept for reference.
They document why each behaviour exists — the Meade command serialization, the
DEC homing-offset conventions, the Ekos PAA log handling and the KStars
extension discovery rules. The summarised history is in `../CHANGELOG.md`.
